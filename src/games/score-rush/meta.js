// Short descriptions for Score Rush. Option keys match the setup panel's
// data-field names. Full rules live in rules.md.

export const meta = {
    short: 'Race to a target score — first to reach it wins. A friendly, no-checkout alternative to X01.',
    aka: ['High Score'],
    options: {
        targetScore: 'The score to reach first (300 is a common target). No exact finish — reach or pass it to win.',
        bullMode: 'Bull scoring: 25 outer / 50 inner (standard), or 50 / 50.',
        singlesOnly: 'Count doubles and trebles as their face (single) value — a gentler game. Bull scoring is unchanged.',
    },
};
